# Django + PostgreSQL

AppThrust application template. The container listens on port 8000.

## Local Docker check

```bash
docker build -t template-django:local .
docker run --rm -p 8000:8000 template-django:local
```
